---
const { returnTo } = Astro.params;
---

<main>
	<div class="banner">
		<h4>404</h4>
		<div>
			<div>deja vu!</div>
			<div>Seems there was glitch in the Matrix!</div>
		</div>
	</div>

	<button type="button" style="color: rgba(255, 0, 0, 0.356);"><a href="/">Return home</a></button>
	<button type="button" style="color: rgba(0, 0, 255, 0.356);"><a href={returnTo}>Return to previous page</a></button>
</main>

<style lang="scss">
	main {
		font-size: 1rem;
		display: flex;
		flex-direction: column;
		align-items: flex-end;
	}

	.banner {
		align-self: center;
		display: flex;
		align-items: center;
		column-gap: 25px;
		font-size: 25px;
		height: 80dvh;

		h4 {
			grid-row: span 2;
			justify-self: center;
		}
	}

	button {
		margin-inline: 25px;
		padding-inline: 16px;
		padding-block: 8px;
		border-radius: 25px;
		background-color: #00000025;
		border: none;
		text-align: center;

		& a {
			color: inherit;
			text-align: center;
			stroke: black;
			stroke-width: 1px;

			&::after {
				content: none;
			}
		}
	}
</style>
